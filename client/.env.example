# Only needed for a SPLIT deploy (static client on one host, server on another).
# Set this to the server's public URL, e.g. https://debateguard-server.onrender.com
# Leave unset for local dev (the Vite proxy handles it) or all-in-one hosting.
VITE_SERVER_URL=
